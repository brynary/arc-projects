{
  "command": "COUNT=$(cat .workflow/improve_counter 2>/dev/null || echo 0); COUNT=$((COUNT + 1)); echo $COUNT > .workflow/improve_counter; if [ $COUNT -lt 20 ]; then echo \"$COUNT/20 — continue\"; else echo 0 > .workflow/improve_counter; echo \"$COUNT/20 — done\"; exit 1; fi",
  "language": "shell",
  "timeout_ms": null
}